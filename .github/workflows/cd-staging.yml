name: CD Staging

on:
  workflow_dispatch:
  push:
    branches: [develop]

concurrency:
  group: cd-staging
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to staging host via SSH
        if: ${{ secrets.STAGING_HOST != '' }}
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: 22
          script_stop: true
          script: |
            set -e
            export APP_DIR="${{ secrets.STAGING_APP_DIR }}"
            export BRANCH="develop"
            if [ ! -d "$APP_DIR/.git" ]; then
              echo "App directory is not a git repo: $APP_DIR"
              exit 1
            fi
            cd "$APP_DIR"
            git fetch --all
            git checkout "$BRANCH"
            git reset --hard "origin/$BRANCH"
            chmod +x infrastructure/scripts/deploy.sh || true
            ./infrastructure/scripts/deploy.sh staging

      - name: Skip deployment notice
        if: ${{ secrets.STAGING_HOST == '' }}
        run: |
          echo "STAGING_HOST secret is not configured in GitHub repository secrets. SSH deployment skipped."

      - name: Health check
        if: ${{ secrets.STAGING_HOST != '' && secrets.STAGING_HEALTHCHECK_URL != '' }}
        run: |
          curl -fsS "${{ secrets.STAGING_HEALTHCHECK_URL }}" >/dev/null
