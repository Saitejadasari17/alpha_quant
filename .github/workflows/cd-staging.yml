name: CD Staging

on:
  workflow_dispatch:
  push:
    branches: [develop]

concurrency:
  group: cd-staging
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check deployment secrets
        id: check_secrets
        run: |
          if [ -z "${{ secrets.STAGING_HOST }}" ]; then
            echo "::warning ::STAGING_HOST is not set in GitHub secrets. Skipping SSH deployment."
            echo "has_secrets=false" >> $GITHUB_OUTPUT
          else
            echo "has_secrets=true" >> $GITHUB_OUTPUT
          fi

      - name: Deploy to staging host via SSH
        if: steps.check_secrets.outputs.has_secrets == 'true'
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: 22
          script_stop: true
          script: |
            set -e
            export APP_DIR="${{ secrets.STAGING_APP_DIR }}"
            export BRANCH="develop"
            if [ ! -d "$APP_DIR/.git" ]; then
              echo "App directory is not a git repo: $APP_DIR"
              exit 1
            fi
            cd "$APP_DIR"
            git fetch --all
            git checkout "$BRANCH"
            git reset --hard "origin/$BRANCH"
            chmod +x infrastructure/scripts/deploy.sh || true
            ./infrastructure/scripts/deploy.sh staging

      - name: Health check
        if: steps.check_secrets.outputs.has_secrets == 'true' && secrets.STAGING_HEALTHCHECK_URL != ''
        run: |
          curl -fsS "${{ secrets.STAGING_HEALTHCHECK_URL }}" >/dev/null
